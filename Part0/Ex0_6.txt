sequenceDiagram
    participant browser
    participant server

    note right of browser: The browser uses JS code to construct a JSON variable containing the note and the date
    note right of browser: The browser adds the new note to a "notes" array
    note right of browser: The browser dynamically redraws the notes object without reloading the page

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    note left of server: The server updates data.json
    server-->>browser: 201 response (note created)
    deactivate server
